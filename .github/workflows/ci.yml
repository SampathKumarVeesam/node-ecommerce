name: Node.js CI Pipeline

on:
  push:
    branches:
      - main
      - feature/**

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Step 1 - Print a welcome message
        run: echo "GitHub Actions has Started"